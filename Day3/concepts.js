// Understanding Object in javascript

const width = 30;

const add = (a, b) => a + b;

const obj = {
  color: "red",
  height: 10,
  "full name": "abhinandan mishra",
  width,
  add
};

console.log(obj, typeof obj);
